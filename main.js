// main.js

document.addEventListener('DOMContentLoaded', () => {
    console.log('DOM cargado.');
    // Inicialización de eventos y funciones globales
});

// Funciones globales
function mostrarMensaje(mensaje) {
    alert(mensaje);
}